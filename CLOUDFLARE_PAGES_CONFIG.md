# Cloudflare Pages详细配置指南

本指南专注于为游戏网站项目配置Cloudflare Pages，包括具体的设置步骤、路由配置和优化建议。

## 1. Cloudflare Pages项目设置详情

### 1.1 基本配置

当您在Cloudflare Pages中设置游戏项目时，请使用以下配置：

- **项目名称**: `game-website` (或保持默认的`game`)
- **生产分支**: `main`
- **预览分支**: 可以选择自动预览所有Pull Request

### 1.2 构建配置

根据项目的`package.json`和`cloudflare-pages-config.json`，使用以下构建设置：

- **构建命令**: `npm run build`
- **构建输出目录**: `dist`
- **构建环境**: Node.js version 18或更高版本

### 1.3 环境变量配置

如果您的游戏网站需要环境变量，请在Cloudflare Pages设置中添加以下变量：

1. 在项目设置中，导航到「环境变量」部分
2. 添加必要的环境变量，例如：
   - `VITE_API_URL`: 游戏API的URL（如果适用）
   - `VITE_GAME_ASSETS_URL`: 游戏资源的基础URL

## 2. 高级配置

### 2.1 路由配置

Cloudflare Pages支持自定义路由规则，以下是推荐的配置（基于您项目的需求）：

1. 在项目设置中，导航到「函数」→「路由规则」
2. 添加以下路由规则：

   | 路由模式 | 目标 | 状态码 | 备注 |
   |---------|------|-------|------|
   | `/*` | `/index.html` | 200 (重写) | 支持SPA路由 |
   | `/api/*` | `/api/*` | 200 | 保留API路由 |
   | `/assets/*` | `/assets/*` | 200 | 静态资源直接提供 |

### 2.2 缓存设置

为了优化游戏网站的性能，建议配置以下缓存规则：

1. 静态资源（图片、CSS、JavaScript等）：设置较长的缓存时间（7天或更长）
2. HTML页面：设置较短的缓存时间或使用ETag验证
3. API响应：根据数据更新频率设置适当的缓存时间

### 2.3 自定义域名设置

当您的网站准备好投入生产使用时，可以添加自定义域名：

1. 在项目设置中，导航到「自定义域名」部分
2. 点击「设置自定义域名」按钮
3. 输入您的域名（例如：`game.yourdomain.com`）
4. 按照提示在您的域名注册商处添加必要的DNS记录
5. 等待DNS传播完成（通常需要几分钟到几小时）
6. 启用HTTPS（Cloudflare会自动为您的域名提供SSL证书）

## 3. 项目特定配置

### 3.1 游戏资源加载优化

针对游戏网站的特殊需求，建议以下优化：

1. **使用Cloudflare Argo Smart Routing**：如果您的游戏资源分布在全球各地，可以启用此功能以提高全球访问速度
2. **配置Brotli压缩**：在Cloudflare设置中启用Brotli压缩，可显著减小JavaScript和其他文本资源的大小
3. **预加载关键资源**：在HTML中使用`<link rel="preload">`标签预加载重要的游戏资源

### 3.2 安全设置

保护您的游戏网站安全：

1. 在Cloudflare安全设置中启用「自动HTTPS重写」
2. 配置「安全头」以防止常见的Web攻击
3. 考虑启用「Cloudflare Access」以保护管理界面（如果有）

## 4. 与CI/CD工作流集成

Cloudflare Pages可以与您项目中的GitHub Actions工作流集成：

1. 当您在GitHub仓库中推送更改到`main`分支时，Cloudflare Pages将自动触发构建和部署
2. 同时，您的`.github/workflows/ci-cd.yml`工作流也会运行，执行代码检查和测试
3. 如果CI/CD工作流失败，您可以在GitHub Actions日志中查看详细信息

## 5. 监控和日志

### 5.1 部署历史

在Cloudflare Pages项目页面，您可以查看所有部署的历史记录，包括：

- 部署状态（成功/失败）
- 部署时间
- 部署的提交信息
- 部署日志

### 5.2 访问分析

启用Cloudflare Analytics以监控您的游戏网站性能和访问情况：

1. 在项目设置中，导航到「分析」部分
2. 启用必要的分析功能
3. 查看访问量、页面加载时间、错误率等关键指标

## 6. 故障排除指南

### 6.1 常见部署问题

| 问题 | 可能原因 | 解决方案 |
|------|---------|--------|
| 构建失败 | 依赖安装错误 | 确保`package.json`中的依赖配置正确，尝试更新npm版本 |
| 静态资源不加载 | 路径错误 | 检查资源引用路径是否正确，确保构建输出目录包含所有资源 |
| 路由不工作 | SPA配置问题 | 确保添加了正确的路由规则`/*`指向`/index.html` |
| 游戏资源加载缓慢 | 资源过大或分布不合理 | 优化资源大小，考虑使用Cloudflare的缓存和CDN功能 |

### 6.2 调试技巧

1. 查看详细的构建日志以识别具体错误
2. 使用Cloudflare的「开发模式」临时禁用缓存进行测试
3. 检查浏览器控制台是否有JavaScript错误
4. 验证网络请求是否成功，特别是API调用和资源加载

## 7. 高级部署选项

### 7.1 部署钩子

如果需要从其他系统触发Cloudflare Pages部署，可以使用部署钩子：

1. 在项目设置中，导航到「部署钩子」部分
2. 创建一个新的部署钩子，设置名称和分支
3. 使用生成的URL通过HTTP请求触发部署

### 7.2 预览环境

Cloudflare Pages自动为每个Pull Request创建预览环境：

1. 当有人提交Pull Request到您的仓库时，Cloudflare会自动构建并部署到预览URL
2. 预览URL会在Pull Request评论中自动添加
3. 您可以在预览环境中测试更改，然后再合并到主分支